import styled from 'styled-components';
import {
  PlayerRequestCardWrapper,
  PlayerRequestCardImage,
  PlayerNameWrapper,
  PlayerName,
  Options,
  OptionButton,
} from '../PlayerRequestCard/playerRequestCard.styles';

export const TeamRequestCardWrapper = styled(PlayerRequestCardWrapper)``;
export const TeamRequestCardImage = styled(PlayerRequestCardImage)``;
export const TeamNameWrapper = styled(PlayerNameWrapper)``;
export const TeamName = styled(PlayerName)``;
export const TeamOptions = styled(Options)``;
export const TeamOptionButton = styled(OptionButton)``;
