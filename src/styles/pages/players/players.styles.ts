import styled from 'styled-components';

import { TeamsWrapper, SectionTitle } from '../teams/teams.styles';

export const PlayersWrapper = styled(TeamsWrapper)``;
export const PlayersSectionTitle = styled(SectionTitle)``;
