import styled from 'styled-components';

import { TeamByIdWrapper, TeamByIdInfoWrapper } from '../team/teamById.styles';

export const PlayerByIdWrapper = styled(TeamByIdWrapper)``;
export const PlayerByIdInfoWrapper = styled(TeamByIdInfoWrapper)``;
